<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/black"
    android:paddingTop="48dp"
    android:paddingBottom="64dp"
    android:paddingLeft="24dp"
    android:paddingRight="24dp">

    <!-- Settings button at top right -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/settingsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:text="@string/activity_start_settings_button"
            android:textSize="24sp"
            android:backgroundTint="@android:color/transparent"
            android:minWidth="48dp"
            android:minHeight="48dp" />
    </RelativeLayout>

    <!-- Logo oben -->
    <ImageView
        android:src="@drawable/sortasong_logo_with_name"
        android:contentDescription="@string/app_name"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_gravity="start"
        android:scaleType="fitCenter"
        android:adjustViewBounds="false"/>

    <!-- Games List with Verification Status -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/gamesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"
        android:layout_marginTop="16dp"
        android:background="@android:color/black"
        android:padding="8dp" />

    <!-- Hinweis -->
    <TextView
        android:id="@+id/folderHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"/>

    <!-- Button: Mit Karten -->
    <Button
        android:id="@+id/withCardsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activity_start_with_cards"
        android:layout_marginBottom="8dp"
        android:textSize="16sp"
        android:backgroundTint="@color/sortasong_primary" />

    <!-- Button: Ohne Karten -->
    <Button
        android:id="@+id/withoutCardsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activity_start_without_cards"
        android:layout_marginBottom="8dp"
        android:textSize="16sp"
        android:backgroundTint="@color/sortasong_primary" />

    <!-- Button: Eigene Spiele verwalten -->
    <Button
        android:id="@+id/manageCustomGamesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activity_start_manage_custom_games"
        android:textSize="16sp"
        android:backgroundTint="@android:color/darker_gray" />

</LinearLayout>

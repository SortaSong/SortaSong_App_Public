<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:paddingTop="48dp"
    android:paddingBottom="64dp"
    android:paddingLeft="24dp"
    android:paddingRight="24dp">




    <!-- UI-Elemente über dem Hintergrund -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        >
        <FrameLayout
            android:id="@+id/turntableContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginBottom="16dp"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/vinylView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:contentDescription="@string/vinyl_record_spinning"
                android:src="@drawable/sortasound_vinyl" />

            <ImageView
                android:id="@+id/armView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"

                android:layout_gravity="bottom|end"
                android:contentDescription="@string/record_player_arm"
                android:src="@drawable/sortasound_playarm"/>
        </FrameLayout>

        <SeekBar
            android:id="@+id/seekBar"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginVertical="16dp"
            android:progressTint="@color/sortasound_secondary"
            android:thumbTint="@color/sortasound_secondary"
            android:progressBackgroundTint="@color/white"
            />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="32dp">

            <Button
                android:id="@+id/playButton"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="▶️"
                android:backgroundTint="@color/sortasound_primary"/>

            <Button
                android:id="@+id/pauseButton"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="⏸️"
                android:layout_marginStart="16dp"
                android:backgroundTint="@color/sortasound_primary"/>
        </LinearLayout>

        <Button
            android:id="@+id/nextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nächster Song (Scannen)"
            android:backgroundTint="@color/sortasound_primary"/>
    </LinearLayout>

</FrameLayout>
